# OmniRoute 專案目錄結構（摘要）

> **根目錄**：`總大腦加免費token/OmniRoute`（D 槽；路徑見實例錨點，本文不寫死磁碟代號）  
> **本檔**：摘要版（方案 A · A1）  
> **完整原始目錄樹**：見同目錄 `W2_T1_目錄結構_FULL.md`（約 17,026 行 / 1.2MB；已列入根 `.gitignore`，僅本機保留）

*Generated: auto · 摘要壓縮：2026-07-12*

---

## 掃描規模

| 項 | 值 |
|----|-----|
| 原始 FULL 行數 | ~17,026 |
| 原始 FULL 大小 | ~1.2MB |
| 頂層目錄 | 21 |
| 頂層檔案 | 58 |
| 專案類型 | Next.js / Node（含 Electron、skills、tests） |

---

## 頂層目錄（有意義結構）

```
OmniRoute/
├── .build/          # Next 建置／dev cache（體積大，掃描噪音主來源）
├── .github/         # CI / workflows
├── .husky/          # git hooks
├── .source/         # 來源輔助
├── .vale/           # 文案 lint
├── .vscode/         # 編輯器設定
├── @omniroute/      # 套件／workspace 範圍
├── bin/             # 運維腳本
├── config/          # 執行設定
├── contrib/         # 貢獻／外掛
├── docs/            # 文件
├── electron/        # 桌面殼
├── examples/        # 範例
├── images/          # 靜態圖
├── logs/            # 執行日誌
├── open-sse/        # SSE 相關
├── public/          # 靜態公開資源
├── scripts/         # 工具腳本
├── skills/          # skill 卡
├── src/             # 主程式
└── tests/           # 測試
```

---

## 頂層關鍵檔案（節選）

| 檔案 | 備註 |
|------|------|
| `package.json` / `package-lock.json` | Node 依賴入口 |
| `next.config.mjs` | Next 設定 |
| `docker-compose.yml` / `Dockerfile` | 容器部署 |
| `Start-OmniRoute.bat` | Windows 啟動 |
| `AGENTS.md` / `README.md` / `CLAUDE.md` | Agent／人類說明 |
| `.env.example` | 環境變數範本（勿把 `.env` 入庫） |
| `vitest.config.ts` / `playwright.config.ts` | 測試設定 |
| `design.md` / `DESING.md` | 設計稿（注意檔名拼寫重複） |

完整根檔清單見 FULL 檔末段（約 58 個頂層 `[FILE]`）。

---

## 摘要觀察（目錄掃描角度）

| 嚴重度 | 觀察 |
|--------|------|
| INFO | `.build/` 含大量 turbopack／cache／`.sst` 產物，導致 FULL 樹膨脹 |
| INFO | 根目錄同時存在 `design.md` 與 `DESING.md`（命名不一致） |
| INFO | Node 專案：無 Python `requirements.txt`／`pyproject.toml` 屬預期（詳見 W2_T4） |
| INFO | 功能性／語法問題彙整 → [W2_T4_問題報告.md](./W2_T4_問題報告.md) |

---

## 相關報告

| 檔案 | 用途 |
|------|------|
| [W2_T2_Python檢查.md](./W2_T2_Python檢查.md) | Python 語法／import |
| [W2_T3_Config檢查.md](./W2_T3_Config檢查.md) | JS／Config |
| [W2_T4_問題報告.md](./W2_T4_問題報告.md) | Wave 2 問題彙整 |
| `W2_T1_目錄結構_FULL.md` | 原始完整目錄樹（本機／gitignore） |
