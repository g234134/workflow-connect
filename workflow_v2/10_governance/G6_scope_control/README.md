# G6 — Scope Control

> **状态**：G6-1／G6-2 已定稿（Scope Control 模块文字层完成）。  
> **禁止**：总控 chat 覆写模块正文（除对账修正票）。

## 产物

| 文件 | 票号 | 状态 |
|------|------|------|
| `10_change_classes.md` | G6-1 | 已定稿（CHG-* 枚举 + §2 命名边界） |
| `20_allowed_actions.md` | G6-2 | 已定稿（§4 主表 + §5 例外／升级；ACT-* 动作词表） |

## 阅读顺序

1. **G6-1** — 选型：`primary_change_class` 是什么。  
2. **G6-2** — 派工：谁可 PATCH／何时必 guard／checker、路径模式、能否标 `DONE`。  
3. **G10** — 边界与禁止盲信（引用上两档，不重定义 CHG）。
