# P8.9-T2 — Feedback ingest + downstream ack v1



> **Ticket**: P8.9-T2-feedback-ingest-and-downstream-ack-v1  

> **Status**: implemented (2026-06-19)  

> **Scope**: ack writer, pending ingest, consumer merge, audit gaps, operator CLI (+ T1 inspect CLI closure)



## Deliverables



| Path | Action |

|------|--------|

| `delivery/feedback_ingest_v1.py` | created |

| `delivery/workflow_event_consumer_v1.py` | created (T1 baseline + T2 ack merge) |

| `scripts/run_feedback_ingest.py` | created |

| `scripts/inspect_workflow_events.py` | created (T1 read-only CLI; T2 closure) |

| `audit/audit_investigation_projection_v1.py` | extended gaps + workflow_notifications section |

| `scripts/run_agent_audit_quickview.py` | wires consumer into wire payload |

| docs + matrix + onboarding | extended per ticket |



## Verification



```bash

python -m unittest tests.test_feedback_ingest_v1 tests.test_workflow_event_consumer_v1 -v

python scripts/run_feedback_ingest.py --case-ref demo_phase --dry-run

python scripts/inspect_workflow_events.py --case-ref demo_phase --format json

python scripts/inspect_workflow_events.py --case-ref demo_phase --format text

python scripts/run_agent_audit_quickview.py --case-ref demo_phase --view investigation --format json

```



## Notes



- Gateway emit path unchanged (`notification_gateway_v1.py` not modified).

- `tracking_status`: notification rows → `pending_ack` | `acked` | `failed`; checkpoint rows → `recorded`.



---



## B_REPORT



- **changed_files**:

  - `delivery/feedback_ingest_v1.py`（T2 主體）

  - `delivery/workflow_event_consumer_v1.py`（T1 baseline + T2 ack merge）

  - `scripts/run_feedback_ingest.py`（T2 operator CLI）

  - `scripts/inspect_workflow_events.py`（T1 read-only CLI；本輪補完）

  - `audit/audit_investigation_projection_v1.py`（gaps + workflow_notifications）

  - `scripts/run_agent_audit_quickview.py`（consumer wire）

  - `tests/test_feedback_ingest_v1.py`（T2 unit）

  - `tests/test_workflow_event_consumer_v1.py`（consumer unit + `test_inspect_cli_json_output_shape`）

  - `04_Workflows/onboarding/standard-case-hitl-resume-notify_guide.md`（inspect CLI 範例）

  - `04_Workflows/testing/standard-case-hitl-resume-notify-matrix.md`（F-1…F-5 tools/commands 欄）

  - `04_Workflows/tickets/P8.9-T2-feedback-ingest-and-downstream-ack-v1_state.md`（本檔）

- **artifacts**: ack files under `outbox/feedback/<case_ref>/acks/`; consumer read model via CLI JSON

- **verification**:

  - `python -m unittest tests.test_feedback_ingest_v1 tests.test_workflow_event_consumer_v1 -v` → 14 tests OK（含 `test_inspect_cli_json_output_shape`）

  - `python scripts/run_feedback_ingest.py --case-ref demo_phase --dry-run` → exit 0

  - `python scripts/inspect_workflow_events.py --case-ref demo_phase --format json` → exit 0; payload含 `events[]`、`timeline[]`

  - `python scripts/inspect_workflow_events.py --case-ref demo_phase --format text` → exit 0

  - `python scripts/run_agent_audit_quickview.py --case-ref demo_phase --view investigation --format json` → exit 0

- **behavior_notes**:

  - `inspect_workflow_events.py` 唯讀：合併 `notification_events.jsonl`、`checkpoint_events.jsonl`、feedback ack；不寫入任何 sink

  - JSON 輸出形狀對齊 P8.9 read model：`ok`, `read_only`, `schema_version`, `case_ref`, `events[]`, `timeline[]`, `count`, `count_by_event_type`, `streams_read`

  - T2 ack merge 後 notification 列 `tracking_status` 為 `pending_ack` | `acked` | `failed`；checkpoint 列維持 `recorded`

- **deferred_items**:

  - P8.9-T3 dispatch handler registry + post-emit webhook wiring（見 plan §4.2）

- **linked_in_dashboard**: `docs/WAVE_PROGRESS_DASHBOARD.md` §Phase 7.5 + P8.9 能力摘要（2026-06-19 Scribe 收錄）

---

## Wave Master schema append（W3-P89-SSOT · 2026-07-10 · 末尾追加 · 不刪歷史）

```yaml
overall_status: done
lifecycle_phase: O
wave_id: W3
phase_targets: [P8.9]
implementation_status: implemented · review_closed_historical
deferred_items:
  - none_on_T2_scope — sibling P8.9-T4 HTTP webhook deferred（見 T3/REG）
  # 歷史 B_REPORT 曾列「T3 deferred」；T3 已於 2026-06-19 implemented · 以本欄為準
observability:
  verify_commands:
    - "python scripts/run_feedback_ingest.py --case-ref demo_phase --dry-run"
    - "python scripts/inspect_workflow_events.py --case-ref demo_phase --format json"
    - "python scripts/run_p8_9_verification_bundle_v1.py --case-ref demo_phase --format json"
  contract_ref: docs/p8_p89_delivery_observability_contract_v1.md
  evidence_index_ref: docs/p8_p89_evidence_index_v1.md
  evidence_tier: L-local
non_claims:
  - T2 implemented ≠ P8.9-T4 webhook live
  - L-local green ≠ prod gate / required CI / GA pass
  - 敘事對齊 ≠ Phase% 上調
alignment_ticket: W3-P89-SSOT-state-dashboard-alignment-v1
```

