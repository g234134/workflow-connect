# =============================================================================
# 大唐三省六部 — 环境变量模板
# 复制为 .env 并填入真值；切勿将含密钥的 .env 提交版控。
# =============================================================================

# =============================================================================
# 【必填】基础 API Keys
# =============================================================================
OPENAI_API_KEY=sk-your-openai-api-key-here
GROQ_API_KEY=gsk-your-groq-api-key-here
TELEGRAM_BOT_TOKEN=your-bot-token:here

# =============================================================================
# 【必填】数据库连接
# =============================================================================
DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/DBNAME

# =============================================================================
# 【选填】基础设施
# =============================================================================
QDRANT_URL=http://127.0.0.1:6333
OPENAI_EMBED_MODEL=text-embedding-3-small

# =============================================================================
# 【选填】FastAPI CORS (dev only)
# =============================================================================
DEV_ALLOW_ALL_ORIGINS=true
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500

# =============================================================================
# Package A — Observability（默认关闭）
# =============================================================================
GOV_CORE_OBSERVABILITY_V2=false
# gov-trace-v2 JSON logs on HTTP (FastAPI); see docs/observability.md
GOV_TRACE_MIDDLEWARE_ENABLED=0
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_TRACING_ENABLED=true
# Phase 5 PG ingest + scheduled alerts (dark gov_core_system)
GOV_CORE_MONITORING_INGEST_ENABLED=0
GOV_CORE_ALERT_NOTIFIER=mock
GOV_CORE_ALERT_COOLDOWN_MINUTES=15

# =============================================================================
# Package B — Budget / Cost Control（默认关闭）
# =============================================================================
GOV_CORE_BUDGET_ENABLED=false
GOV_CORE_DAILY_BUDGET_USD=50.0
GOV_CORE_WEEKLY_BUDGET_USD=200.0
GOV_CORE_PER_TASK_BUDGET_USD=2.0
GOV_CORE_BUDGET_WARN_THRESHOLDS=0.8,0.9,1.0

# =============================================================================
# Package C — Structured Errors（默认关闭）
# =============================================================================
GOV_CORE_STRUCTURED_ERRORS=false

# =============================================================================
# Package D — Retry / DLQ / Interrupt（默认关闭）
# =============================================================================
GOV_CORE_RETRY_POLICY_ENABLED=false
GOV_CORE_RETRY_MAX_ATTEMPTS=3
GOV_CORE_RETRY_BACKOFF_MS=250
GOV_CORE_RETRY_BACKOFF_MULTIPLIER=2
GOV_CORE_DLQ_ENABLED=false
GOV_CORE_DLQ_SINK=all
GOV_CORE_INTERRUPT_ENABLED=false
GOV_CORE_ANSWER_FALLBACK_MODEL=gpt-4o-mini

# =============================================================================
# Workflow 配置
# =============================================================================
GOV_CORE_CHECKPOINTER=auto
GOV_CORE_JSON_MIRROR=1

# =============================================================================
# K-2 Monitoring Graph（默认关闭）
# =============================================================================
GOV_MONITORING_GRAPH_ENABLED=0

# =============================================================================
# API 观察闸门（默认关闭，仅 dev）
# =============================================================================
GOV_CORE_API_EXPOSE_MONITORING_GRAPH=0
GOV_CORE_API_EXPOSE_IBRIDGE=0

# =============================================================================
# 日志级别（local/dev: DEBUG, staging/prod: INFO/WARN）
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# 其他集成（按需要添加）
# =============================================================================
# NVIDIA_API_KEY=nvapi-...
# TAVILY_API_KEY=tvly-...
# FIRECRAWL_API_KEY=fc-...
# JINA_API_KEY=jina_...
# HUGGINGFACE_API_TOKEN=hf_...
