# Minimal dependencies for GitHub Actions dark-tier agent smoke (no PG / no live LLM).
# Aligns with gov_core_system/requirements.txt subset used by unittest mocks.
fastapi>=0.110
pydantic>=2.0
starlette>=0.36
httpx>=0.27
python-dotenv>=1.0
langgraph>=0.2
pyyaml>=6.0
