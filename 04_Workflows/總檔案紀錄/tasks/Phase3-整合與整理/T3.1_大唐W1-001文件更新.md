---
task_id: T3.1
title: 大唐 W1-001 文件更新（cfg/tang.py 已過時）
phase: Phase3-整合與整理
status: done
priority: P1
owner: 天天
wave_ref: [W1-001]
depends_on: []
blocks: [T3.3]
parallel_with: [T1.1, T1.2, T3.2]
tests: [T31a, T31c]
last_updated: 2026-07-06
completed_at: 2026-07-06
---

# T3.1 — 大唐 W1-001 文件更新（cfg/tang.py 已過時）

## 摘要

關閉 W1-001：現行專案**不需要**建立 `cfg/tang.py`；更新過時目錄結構文件，改指向 `gov_paths` / `Master_Map.json` 路徑解析機制。

## 根因分析

原報告 W1-001：「`.env.example` 參考 `cfg/tang.py` 但 `cfg/` 不存在」。

**2026-07-06 複查：**

| 檢查項 | 結果 |
|--------|------|
| `D:\大唐三省六部\cfg\` | **不存在** |
| 程式碼搜尋 `cfg/tang.py`、`tang.py` | **無引用** |
| 現行 `.env.example` | **未提及** `cfg/tang.py` |
| 路徑解析 | `02_Agents_Core\gov_paths.py`、`04_Workflows\_tang_paths.py`、`Master_Map.json` |

**結論：** 問題為 **Wave 1 目錄結構報告過時**（仍列 `cfg/`），非運行時 bug。W1-001 應標記 **RESOLVED（文件修正）**，而非補建 `cfg/tang.py`。

## 影響範圍

| 項目 | 說明 |
|------|------|
| 專案 | 大唐三省六部 |
| 路徑 | `04_Workflows/總檔案紀錄/_archive/Wave1-大唐三省六部/W1_T1_目錄結構.md` 等 |
| 阻斷程度 | **無** — 僅文件／紀錄一致性 |

## 相關任務與依賴

| 關係 | 任務 | 說明 |
|------|------|------|
| 可並行 | [T1.1](../Phase1-立即處理/T1.1_AI_HUB配置編碼修復.md) | 不同專案 |
| 可並行 | [T3.2](./T3.2_crew_demo去重.md) | 不同專案 |
| 關聯 | [T3.3](./T3.3_跨專案冒煙測試.md) | 可含 `gov_paths` 快檢 |

## 修復步驟

### 步驟 1 — 更新 W1 目錄結構報告

編輯 `04_Workflows/總檔案紀錄/_archive/Wave1-大唐三省六部/W1_T1_目錄結構.md`：

- 移除或標記刪除：`cfg/`（系統配置）
- 新增現行結構重點：
  - `01_Environments/` — 環境與 `.env`
  - `02_Agents_Core/` — `gov_paths.py`
  - `04_Workflows/` — `_tang_paths.py`、`Master_Map.json`、`_ops_cycle.py`

### 步驟 2 — 更新 W1 問題報告狀態

編輯 `04_Workflows/總檔案紀錄/_archive/Wave1-大唐三省六部/W1_T3_問題報告.md`：

- W1-001 狀態改為 **已解決（文件過時，不需 cfg/tang.py）**
- 加連結：`→ 任務：../../tasks/Phase3-整合與整理/T3.1_大唐W1-001文件更新.md`

### 步驟 3 — （可選）在專案 README 加一行路徑說明

於 `D:\大唐三省六部\README.md`「Start Here」表後補充：

> 路徑解析請用 `gov_paths.get_tang_gov_root()`，勿引用已移除的 `cfg/tang.py`。

## 驗收測試

### T31a — gov_paths 根目錄解析

**命令：**

```powershell
Set-Location D:\大唐三省六部
python -c "import sys; sys.path.insert(0,'02_Agents_Core'); from gov_paths import get_tang_gov_root; r=get_tang_gov_root(); print(r); assert r.replace('/','\\').lower().endswith('\\大唐三省六部'.lower()) or '大唐三省六部' in r; print('T31a PASS')"
```

**通過標準：** 印出之路徑含「大唐三省六部」。

---

### T31b — ops_cycle minimal checklist

**命令：**

```powershell
Set-Location D:\大唐三省六部
python 04_Workflows\_ops_cycle.py checklist --mode minimal
```

**通過標準：** JSON 輸出 `"ok": true`。

---

### T31c — 文件已更新

**命令：**

```powershell
Select-String -Path "04_Workflows/總檔案紀錄/_archive/Wave1-大唐三省六部/W1_T3_問題報告.md" -Pattern "已解決|RESOLVED" -SimpleMatch
Select-String -Path "04_Workflows/總檔案紀錄/_archive/Wave1-大唐三省六部/W1_T1_目錄結構.md" -Pattern "gov_paths|Master_Map" -SimpleMatch
```

**通過標準：** W1-001 標記已解決；W1_T1 含新路徑關鍵字。

## 回滾方案與風險

還原上述 markdown 編輯即可。

## 完成紀錄與後續建議

| 欄位 | 值 |
|------|-----|
| 執行者 | 天天 |
| 完成日期 | 2026-07-06 |
| 驗收測試 | T31a: ✅ / T31c: ✅（T31b ops_cycle checklist 非強制，跳過） |
| 備註 | W1-001 已解決：cfg/tang.py 確認不存在且無任何引用；W1_T1 目錄結構報告已更新指向 gov_paths / Master_Map.json；W1_T3 問題報告已標記已解決。 |
