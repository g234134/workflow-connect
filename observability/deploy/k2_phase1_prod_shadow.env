# K-2 Phase 1 prod shadow — deployment-layer only (do NOT commit secrets here).
# HQ-GOV-K2-P1-SHADOW-20260525 · T+0 2026-05-26 06:00 UTC
#
# Scope (this ticket): LOCAL prod-like rehearsal only — 戰車根 + gov_core_system venv.
# Overlay before local uvicorn reload; do not merge into repo .env.
# Remote prod / K8s / systemd on cloud hosts: OUT OF SCOPE until separate
#   "K2 Phase 1 prod shadow – remote rollout" ticket with explicit deploy paths.

GOV_DEPLOY_ENV=production
IBRIDGE_EXPORT_ALLOW_PRODUCTION=1
IBRIDGE_EXPORT_ENABLED=1
GOV_K2_PROD_SHADOW=1
