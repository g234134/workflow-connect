---
task_id: T3.3
title: 跨專案冒煙測試
phase: Phase3-整合與整理
status: done
priority: P1
owner: 天天
wave_ref: []
depends_on: [T1.1, T1.2, T2.1, T3.1, T3.2]
blocks: []
parallel_with: []
tests: [T33a, T33b, T33c, T33d, T33e, T33f, T33g]
last_updated: 2026-07-08
completed_at: 2026-07-08
---

# T3.3 — 跨專案冒煙測試

## 摘要

在 Phase 1–2 關鍵任務完成後，對 D 槽主要專案執行一輪 **端到端冒煙測試**，確認編碼、API、路徑解析、專案可執行性。

## 根因分析

總調度分析分 Wave 獨立完成，缺少 **整合驗收**。本任務作為發布中心收尾，將分散修復合併為可重複執行的 smoke script 清單。

## 影響範圍

| 項目 | 說明 |
|------|------|
| 專案 | 全部主要 D 槽專案 + C 槽 AI 戰車 + OmniRoute |
| 阻斷程度 | **驗收層** — 不通過則回退至對應 Phase 任務 |

## 相關任務與依賴

| 關係 | 任務 | 說明 |
|------|------|------|
| 前置 `depends_on` | [T1.1](../Phase1-立即處理/T1.1_AI_HUB配置編碼修復.md) | D 槽 config 須可 load；**T1.1 config 層已於 2026-07-06 完成**（T11a–T11d 通過），可作為 T33b 前置條件；main.py runtime 見 [T1.3](../Phase1-立即處理/T1.3_AI_HUB_venv與crewai相容性.md) |
| 前置 `depends_on` | [T2.1](../Phase2-OmniRoute/T2.1_Chat-Completions路由與Provider.md) | chat 須 200 |
| 軟依賴 | [T1.2](../Phase1-立即處理/T1.2_端口與服務對照澄清.md) | 端口認知 |
| 軟依賴 | [T2.2](../Phase2-OmniRoute/T2.2_壓縮管線驗證.md) | 可選納入 |
| 軟依賴 | [T3.1](./T3.1_大唐W1-001文件更新.md) | 可並行 |
| 軟依賴 | [T3.2](./T3.2_crew_demo去重.md) | 可並行 |

## 修復步驟

本任務**無修復**，僅執行下方驗收測試。建議一次跑完並將輸出存檔：

```powershell
$log = "04_Workflows/總檔案紀錄/tasks/_artifacts\smoke_$(Get-Date -Format yyyyMMdd_HHmmss).log"
New-Item -ItemType Directory -Force -Path "04_Workflows/總檔案紀錄/tasks/_artifacts" | Out-Null
# 各 T33x 測試輸出可 Tee-Object -FilePath $log
```

## 驗收測試

### T33a — AI 戰車 config（C 槽）

**命令：**

```powershell
Set-Location C:\Users\666LAG
python -c "from config_loader import initialize_config_system; assert initialize_config_system(); print('T33a PASS')"
```

**通過標準：** 初始化 True。

---

### T33b — AI_HUB config（D 槽，依賴 T1.1）

**命令：**

```powershell
Set-Location C:\Users\666LAG
python -c "from config_loader import initialize_config_system; assert initialize_config_system(r'D:\AI_HUB\config'); print('T33b PASS')"
```

**通過標準：** D 槽 config 初始化 True（T1.1 完成後）。

---

### T33c — OmniRoute API + Chat（依賴 T2.1）

**命令：**

```powershell
$r1 = Invoke-WebRequest "http://127.0.0.1:20128/v1/models" -UseBasicParsing
$body = '{"model":"auto/best-coding","messages":[{"role":"user","content":"Say OK"}],"max_tokens":8,"stream":true}'
$r2 = Invoke-WebRequest "http://127.0.0.1:20128/v1/chat/completions" -Method POST -Body $body -ContentType "application/json" -UseBasicParsing -TimeoutSec 60
if ($r1.StatusCode -eq 200 -and $r2.StatusCode -eq 200) { Write-Host "T33c PASS" }
```

**通過標準：** models 與 chat 均 200。

---

### T33d — 大唐 gov_paths + checklist

**命令：**

```powershell
Set-Location D:\大唐三省六部
python -c "import sys; sys.path.insert(0,'02_Agents_Core'); from gov_paths import get_tang_gov_root; print(get_tang_gov_root())"
python 04_Workflows\_ops_cycle.py checklist --mode minimal | Select-String '"ok": true'
Write-Host "T33d PASS"
```

**通過標準：** 路徑印出 + checklist ok。

---

### T33e — AI_HUB API key probe（不暴露 key）

**命令：**

```powershell
Set-Location D:\AI_HUB
python test_api_keys.py
Write-Host "T33e DONE (review OK/FAIL lines manually)"
```

**通過標準：** 腳本正常結束；已配置 key 的項目顯示 OK（未配置可 FAIL，不阻斷本項）。

---

### T33f — crew_demo 語法（canonical 路徑）

**命令：**

```powershell
python -m py_compile "D:\我的專案\crew_demo\main.py"
Write-Host "T33f PASS"
```

**通過標準：** 無 compile 錯誤。

---

### T33g — 一鍵彙總腳本（可選）

**命令：**

```powershell
$results = @()
function Test-Step($name, [scriptblock]$sb) {
  try { & $sb; $results += [pscustomobject]@{ Test=$name; Result='PASS' } }
  catch { $results += [pscustomobject]@{ Test=$name; Result='FAIL'; Error=$_.Exception.Message } }
}
Test-Step 'T33a' { Set-Location C:\Users\666LAG; python -c "from config_loader import initialize_config_system; assert initialize_config_system()" }
Test-Step 'T33b' { Set-Location C:\Users\666LAG; python -c "from config_loader import initialize_config_system; assert initialize_config_system(r'D:\AI_HUB\config')" }
Test-Step 'T33c' {
  Invoke-WebRequest "http://127.0.0.1:20128/v1/models" -UseBasicParsing | Out-Null
  $b = '{"model":"auto/best-coding","messages":[{"role":"user","content":"hi"}],"max_tokens":5,"stream":true}'
  Invoke-WebRequest "http://127.0.0.1:20128/v1/chat/completions" -Method POST -Body $b -ContentType "application/json" -UseBasicParsing -TimeoutSec 60 | Out-Null
}
Test-Step 'T33d' { Set-Location D:\大唐三省六部; python 04_Workflows\_ops_cycle.py checklist --mode minimal | Out-Null }
Test-Step 'T33f' { python -m py_compile "D:\我的專案\crew_demo\main.py" }
$results | Format-Table -AutoSize
$fail = ($results | Where-Object Result -eq 'FAIL').Count
if ($fail -eq 0) { Write-Host "T33g ALL PASS" } else { Write-Host "T33g FAIL count:" $fail }
```

**通過標準：** 所有子項 PASS（T33e 可手動除外）。

## 回滾方案與風險

本任務**無修復**，僅執行驗收測試；若子項失敗，回退至對應 Phase 任務重新修復後再跑。

## 完成紀錄與後續建議

| 欄位 | 值 |
|------|-----|
| 執行者 | Cursor Agent |
| 完成日期 | 2026-07-08 |
| 驗收測試 | T33a: ✅ / T33b: ✅ / T33c: ✅ / T33d: ✅ / T33e: ✅ / T33f: ✅ / T33g: ✅ |
| 失敗項 → 回退任務 | 無 |
| 備註 | T33d PowerShell 中文路徑比對曾誤判，Python 斷言通過。T33e NVIDIA NIM 403 為 key 授權問題，不阻斷。日誌：`_artifacts/smoke_20260708_final.log` |
