# WAVE 1 — T2: Python/JS 語法及完整性檢查

## 方法

針對大唐三省六部專案，由於總檔案數達 172,014，採用分層取樣：

1. 頂層核心檔案逐一檢查（README, requirements.txt, .env.example, AGENTS.md）
2. Python 語法抽樣檢查（libs/ 核心模組）
3. 大型資料檔案（data/, contracts/）抽樣驗證 JSON 格式

---

## 核心文件檢查

| 檔案 | 大小 | 狀態 | 說明 |
|------|------|------|------|
| `00_master_plan.md` | ~1.4K | ✅ | 主計畫文件 |
| `AGENTS.md` | ~1.1K | ✅ | AI Agent 指引文件 |
| `README.md` | ~1.1K | ✅ | 專案 README |
| `requirements.txt` | 存在 | ✅ | Python 依賴列表 |
| `.env.example` | 存在 | ✅ | 環境變數範本 |
| `.gitignore` | 存在 | ✅ | Git 忽略規則 |

## Python 語法檢查（核心源碼層）

> 由於專案含大量自動生成的 data/ 目錄 .py 檔案（多為 Enum/資料定義），
> 實際第一方邏輯代碼需經 wheels/ 過濾後再做深度檢查。

| 區域 | 預估檔案數 | 語法狀態 | 說明 |
|------|-----------|---------|------|
| libs/ | 數千 | ⏳ 背景分析中 | 核心程式庫 |
| backend/ | 待確認 | ⏳ 背景分析中 | 後端服務 |
| contracts/ | 大量 | 📝 多為資料定義 | 合約定義非執行邏輯 |
| data/enum/ | 大量 | 📝 多為 Enum 資料 | 自動生成常數定義 |

## JS/前端檢查

| 位置 | 狀態 | 說明 |
|------|------|------|
| frontend/package.json | 待確認 | Vue.js 前端專案 |
| .js 檔案 (2,909個) | ⏳ 背景分析中 | 前端腳本 |

## 完整性觀察

| 問題 | 說明 |
|------|------|
| 專案內含 wheels/（第三方套件） | 可能導致依賴版本混亂，建議使用 requirements.txt + venv 管理 |
| 大量 pyc 檔案 (29,585) | 編譯緩存未清理，可加入 .gitignore |
| log/ 目錄有運行產出 | 正常，但大型 log 可能佔空間 |
