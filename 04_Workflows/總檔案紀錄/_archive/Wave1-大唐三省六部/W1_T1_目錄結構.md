# WAVE 1 — T1: 目錄結構分析報告
## 專案：大唐三省六部（紫塞秋風：三鎮演兵）

> 更新時間: 2026-07-06 19:20

專案類型: MMORPG 遊戲伺服器
框架: Python (主要) + Vue.js (前端) + C/C++ (擴展)
總檔案數: 172,014
總目錄數: 13,880
總大小: 約 37GB+（含遊戲庫）

---

## 主要目錄（實際結構）

```
/D/大唐三省六部/
├── 00_master_plan.md       # 主計畫
├── AGENTS.md               # AI Agent 指引
├── .env.example            # 環境變數範本（參考 cfg/tang.py，但 cfg/ 已不存在）
│
├── 01_Environments/        # 環境設定
├── 02_Agents_Core/         # Agent 核心
├── 02_Data/                # 資料
├── 03_RAG_Database/        # RAG 資料庫
├── 04_Workflows/           # 工作流（含 Master_Map.json v2.66）
│   └── maps/               # 地圖設定（含 Master_Map.json v1.0.0 舊版）
├── 05_Temp_Cache/          # 暫存
├── 06_Exports_Output/      # 匯出輸出
├── 07_Knowledge/           # 知識庫
├── agents/                 # Agent 定義
├── app/                    # 應用程式
├── artifacts/              # 產出物
├── audit/                  # 審計
├── cases/                  # 案例
├── scripts/                # 工具腳本
├── tools/                  # 開發工具
├── docs/                   # 專案文檔
│
├── wheels/                 # 第三方 Python 套件（建議改用 requirements.txt）
└── log/                    # 運行日誌
```

## 檔案類型分布

```
.py     61,871   (Python 源碼，含大量自動生成資料定義)
.json   41,722   (遊戲資料 / 配置)
.pyc    29,585   (Python 編譯字節碼)
.h      17,922   (C/C++ 頭文件)
.js      2,909   (JavaScript/TypeScript 前端)
.md      2,155   (文檔)
.txt       677   (文字資源)
其他: .pyi, .pyd, .html, .css, .sh, .toml, .cfg, .yaml, .proto 等
```

## 結構變更記錄

| 項目 | 說明 |
|------|------|
| `cfg/` | ❌ 已不存在（W1-001 已解決） |
| `libs/`、`backend/`、`frontend/`、`contracts/`、`data/` | 舊版結構，實際已重組為 01–07 編號目錄 |
| `share/`、`prod_cfg/`、`sln/` | 舊版結構，實際已不存在 |
| `04_Workflows/Master_Map.json` | v2.66（主檔，598 行） |
| `04_Workflows/maps/Master_Map.json` | v1.0.0（舊版備份，18 行） |

## 初步問題

| 嚴重度 | 路徑 | 描述 |
|--------|------|------|
| INFO | wheels/ | 第三方套件直接儲存在專案內（非 pip 依賴管理） |
| INFO | log/ | 含運行產生日誌 |
| INFO | 全專案 | 172K 檔案規模極大，含大量 generated/data 檔案 |
